## This APP exposes an API to Extract data from customers, transactions, Items datasets and then transforms and LOAD them into output JSONL files

##To start the server

Get into the folder and Run

create a virtual environment

'''
python -m venv <VENV_NAME>

'''
Get into the venv

Install requirements

'''
pip install -r requirements.txt

'''

Run the application

'''
python main.py

'''
## Prometheus client is running on 8000. Add this to your prometheus server to pull the metrics

## API details can be found at swagger_server/swagger/swagger.yaml

Input: Dict of filenames in csv, json, xml formats. If values are None. default values are taken from config
Output: Location of manifest file

output files are stored in output folder and manifest file is stored in manifest folder.

Config Location: config/config.yaml


